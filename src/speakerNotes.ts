import { dedent } from "@qnighy/dedent";

// Flat array indexed by Spectacle slideIndex.
// Each entry corresponds to one physical <Slide> in order.
// Total: 29 slides
//
// When adding/removing slides, update this array to match.
// Components with multiple slides have multiple entries here.
export const speakerNotes: readonly string[] = [
  // 0: S01Title
  dedent`\
    証明を書くには、ゴールとなる命題がないといけない。
    定式化をすることを考えてみる。`,

  // 1: S02Tools
  dedent`\
    一応断っておくと、ここでは0以上の整数を自然数と呼ぶ。
    これらが「ある」という前提で話を進める。
    いつもの足し算、かけ算だと思ってほしい。`,

  // 2: S03Prerequisites
  dedent`\
    中国剰余定理は後半で重要になる。
    束縛変数・自由変数の区別がわかっていると理解がスムーズ。
    述語と関数の違いも意識しておくとよい。`,

  // 3: S04WhatsMissing（1/2 — 無いもの一覧）
  dedent`\
    足し算とかけ算と論理式しかないのに、こんなに色々な概念が「無い」。
    でも、実はこれらを全部作れる。それがこの発表の趣旨。`,

  // 4: S04WhatsMissing（2/2 — 述語とは）
  dedent`\
    述語は真偽を返すもの、関数は値を返すもの。この区別が重要。
    負の数や分数なども、ペアと同値類で表現できてしまう。後で触れる。`,

  // 5: S05BasicPredicates（1/2 — 定義）
  dedent`\
    比較演算子を存在量化で表現するのがキーアイデア。
    「ある数を足すと等しくなる」ことで不等式を表す。
    素数の定義では「1より大きい約数を持たない」を展開する。`,

  // 6: S05BasicPredicates（2/2 — 展開）
  dedent`\
    許されるのか？ → すべて +, ×, =, 論理記号だけで書けるので、許される。
    展開すると、見た目は複雑だが機械的に変換しただけ。`,

  // 7: S06Functions（1/2 — ドットマイナス）
  dedent`\
    ドットマイナス（truncated subtraction）は、n < m のとき 0 を返す引き算。
    ∃! は「ただひとつ存在する」の意味。
    関数を使うときは、∃!x. P(x) の形で取り出す。`,

  // 8: S06Functions（2/2 — 展開）
  dedent`\
    使う側では ∀x. P(x) → φ(x) と展開する。
    これが「関数適用」に相当する。
    関数を組み合わせると、∀ が入れ子になっていく。`,

  // 9: S07LetIn
  dedent`\
    ∀x. x = t → φ(x) は、プログラミングの let x = t in φ に対応する。
    複雑な式を変数に束縛して読みやすくできる。
    論理式のレベルで変数定義ができるのは便利。`,

  // 10: S08BasicFunctions（1/2 — 問題）
  dedent`\
    割り算は商と剰余のペアで考える。
    ∃! で取り出す側を選ぶことで、商か剰余かを決められる。`,

  // 11: S08BasicFunctions（2/2 — 解答）
  dedent`\
    累乗は難しい。次のセクションで詳しく扱う。
    nより大きい次の素数は、min 述語を使えば取れる。`,

  // 12: S09ExpWall
  dedent`\
    ここが核心。かけ算を n 回繰り返すには、ループが必要。
    でも今の論理式にはループに相当するものがない。
    「まだ」と言ったのは、リストを導入すればできるようになるから。`,

  // 13: S10Pair（1/3 — ペアとは）
  dedent`\
    ペアは多くの言語に組み込みで存在する基本データ構造。
    自然数にエンコードすることで、論理式の世界に持ち込める。`,

  // 14: S10Pair（2/3 — 等濃）
  dedent`\
    ペアと自然数が等濃であることは、|ℕ| = |ℚ| の証明と本質的に同じ手法。
    斜めに数え上げることで全単射を作る。`,

  // 15: S10Pair（3/3 — ペアリング関数）
  dedent`\
    カントールのペアリング関数は、斜めの対角線に沿って数え上げる。
    この関数は全単射であることが証明できる。`,

  // 16: S11PairMake（1/2 — 定義）
  dedent`\
    割り算は使えないので、2n = ... の形にして ∃! で取る。
    これでペアの作成が +, × だけで表現できる。`,

  // 17: S11PairMake（2/2 — 等価性）
  dedent`\
    ペアの等価性 (a,b) = (c,d) ⇒ a = c ∧ b = d も論理式で表現できる。
    展開すると結構大きくなるが、機械的に生成できる。`,

  // 18: S12PairGet
  dedent`\
    ∃x. P(x) や ∀x. P(x) → φ(x) は、条件を満たす値を取ってくる魔法のようなもの。
    ペアのエンコード・デコードが両方できるようになった。`,

  // 19: S13ListMotivation（1/2 — ゲーデル数）
  dedent`\
    ゲーデル数は素因数分解を使うアプローチだが、
    指数を取り出すのに累乗が必要で、鶏と卵の問題になる。`,

  // 20: S13ListMotivation（2/2 — CRT）
  dedent`\
    CRTベースのアプローチなら、剰余だけで済む。
    modを全部素数にすれば互いに素は自動的に満たされる。`,

  // 21: S14ListDefine
  dedent`\
    リストの定義は、l という大きな数と素数 p を使って、
    l を p で割った余りで各要素を取り出す形。
    該当する l が複数あるかもしれないが、
    リストとして使う分には同じ振舞いをするので問題ない。`,

  // 22: S15ExpCalc
  dedent`\
    リストの漸化式として a_{i+1} = a_i × a を指定すれば、
    (0, 1), (1, a), (2, a²), ... というリストが取れる。
    k番目のペアの右側を取り出せば a^k が得られる。`,

  // 23: S16ExpDone（1/2 — フェルマー）
  dedent`\
    フェルマーの最終定理は「n > 2 ならば aⁿ + bⁿ = cⁿ の非自明な解はない」。
    これが +, ×, 論理記号だけで表現できるようになった。
    展開すると巨大な論理式になる。`,

  // 24: S16ExpDone（2/2 — 素因数分解リスト）
  dedent`\
    素因数分解ベースのリストも、累乗ができるようになったので使える。
    上限を気にする必要がなくなる。`,

  // 25: S17Future
  dedent`\
    ゲーデルのβ関数は、上限付きリストのより標準的な構成法。
    中国剰余定理を直接使う形で、私の方法より簡潔。
    不完全性定理は、この体系のなかで自分自身を語れることから導かれる。`,

  // 26: S18Incompleteness（1/2 — 構築）
  dedent`\
    不完全性定理の直観: 自然数論は自分自身を記述できるほど強力。
    「この文は証明できない」という自己言及的な命題が構成できる。
    もし証明できたら矛盾し、証明できないなら真だが証明不能。`,

  // 27: S18Incompleteness（2/2 — 完全性）
  dedent`\
    排中律と完全性の違いに注意。
    ⊢ φ ∨ ¬φ（排中律）は定理として証明できるが、
    ⊢ φ か ⊢ ¬φ のどちらかが成り立つ（完全性）は成り立たない。
    構成主義にも繋がる深い話。`,

  // 28: S19Limits
  dedent`\
    ペアノ算術の証明論的順序数は ε₀。
    ε₀ までの超限帰納法は PA で正当化できるが、
    ε₀ 自身での帰納法は PA では証明できない（ゲンツェンの定理）。
    パリス・ハリントンの定理やグッドスタイン数列など、
    PA で表現可能だが PA では証明不能な命題が実際に存在する。`,
];
